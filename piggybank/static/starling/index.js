document.addEventListener('DOMContentLoaded', () => {
  // when user selects customers, make API call to get savings goals if any
  // always show account balance & money in all goals - checksum
  // if none, display form to create a goals
    /// ...new view & template not here in JS
  // if >1, check
      // display all goals & special display for one with roundup if extsits
      // if roudnup exists, display [it, target, factor, %, name, start date] & DELETE option
        // refresh button (after new transactions) - check sufficient balane etc
      // if no roundup, display form to set up for existing goal
}
